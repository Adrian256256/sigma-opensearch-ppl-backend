| multisearch [search source=windows-process_creation-* | where LIKE(Image, "%\\powershell.exe") OR LIKE(Image, "%\\cmd.exe") OR LIKE(Image, "%\\wscript.exe") OR LIKE(Image, "%\\cscript.exe") OR LIKE(Image, "%\\regsvr32.exe") AND @timestamp >= now() - 60s] [search source=windows-network_connection-* | where Initiated="true" AND (DestinationPort in (4444, 5555, 8080, 9999)) AND @timestamp >= now() - 60s] | stats count() as event_count by process, ComputerName | where event_count >= 2
